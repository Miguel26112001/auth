package com.example.authentication.iam.domain.model.valueobjects;

/**
 * Enumeration of system roles used for authorization and access control.
 */
public enum Roles {

  /** Administrator role with elevated privileges. */
  ROLE_ADMIN,

  /** Standard user role with basic permissions. */
  ROLE_USER
}
